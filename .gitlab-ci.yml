# Disabled for now, but to do: re-enable once working
#variables:
#  INSTANCE: sso-$CI_COMMIT_REF_SLUG
#  VERSION: $CI_COMMIT_BRANCH

include:
  - project: fairdata/fairdata-ci
    ref: master
    file:
      - /templates/deploy.yml
#      - /templates/test_fairdata_e2e.yml
# To include later
#      - /templates/review.yml

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^(master|demo|stable|staging|test)$/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME !~ /^(demo|master|stable|staging|test)$/

stages:
  # Deploy to target environment
  - deploy
  # Test
  - test
  # Test fairdata-e2e tests (Cypress-based)
#  - test_fairdata_e2e

# Replaced by definition in fairdata/fairdata-ci (branch)
#deploy:
#  stage: deploy
#  environment: $CI_COMMIT_REF_NAME
#  tags:
#    - shell
#  script:
#    - ansible-playbook -i $ANSIBLE_INVENTORY $DEPLOY_PLAYBOOK -e "build_id=$CI_COMMIT_REF_SLUG repo_version_app=$CI_COMMIT_REF_NAME"

# Test of application code
run_integration_tests:
  stage: test
  script:
    - ansible-playbook -i $ANSIBLE_INVENTORY $TEST_PLAYBOOK --vault-id $ANSIBLE_VAULT_FILE

sonarqube-check:
  tags:
    - docker
  stage: test
  image: 
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script: 
    - sonar-scanner
  allow_failure: true
  only:
    - merge_requests
    - master
    - test
    - staging
    - stable
    - demo
